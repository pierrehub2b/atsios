# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'atsDriver' do
  inherit! :search_paths
  # Pods for testing
  pod 'Embassy'
  pod 'EnvoyAmbassador'
  pod 'BlueSocket'
end
